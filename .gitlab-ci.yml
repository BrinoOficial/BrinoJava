image: alpine

before_script:
  - apk update -qq && apk install -y -qq openjdk-8-jdk
  - apk install ant
  - apk install tar
  - apk install ncftp
  
stages:
  - compile
  - package

compile:
  stage: compile
  artifacts:
    paths:
      - BrinoIDE.jar
  script:
    - ant
  only:
  - ODB
