image: ubuntu

before_script:
  - apt-get update -qq && apt-get install -qq openjdk-8-jdk
  - apt-get install ant
  - apt-get install tar
  - apt-get install ncftp
  
stages:
  - compile

compile:
  stage: compile
  artifacts:
    paths:
      - BrinoIDE.jar
  script:
    - ant
  only:
  - ODB
