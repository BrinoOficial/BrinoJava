image: alpine

before_script:
  - apk update -qq && apk add -qq openjdk-8-jdk
  - apk add ant
  - apk add tar
  - apk add ncftp
  
stages:
  - compile

compile:
  stage: compile
  artifacts:
    paths:
      - BrinoIDE.jar
  script:
    - ant
  only:
  - ODB
